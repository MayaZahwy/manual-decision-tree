# Comma-separated browser origins allowed to call the API.
# Add your deployed frontend URL when you deploy (e.g. https://your-app.vercel.app).
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Set automatically on Render, Railway, Fly.io, etc. The Procfile uses this port.
# PORT=8000
